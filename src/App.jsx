import React from 'react'

import { runSaga } from 'redux-saga'
import { cancelled } from 'redux-saga/effects'

function App() {
  return <div className="App">If you see this message, `runSaga` has been imported correctly</div>
}

export default App
